#include <iostream>
#include <algorithm>
#include <string>

namespace largeNumbers {
    std::string multiply(std::string a, std::string b);
    std::string add(std::string a, std::string b);
    int ctoi(char num);

    std::string multiply(std::string a, std::string b) {
        std::string result;
        for (int i = 0; i < ; ++i) {

        }
    }

    std::string add(std::string a, std::string b) {

    }

    int ctoi(char num) {
        return num - '0';
    }
}

int main() {
    std::string a;
    std::string b;
    std::cin >> a >> b;
    std::cout << largeNumbers::multiply(a, b);
    return 0;
}
